package world;

final class WorldCamera {
	/* The camera can be defined by a pair of normal vectors.
	 * One vector defines which face the camera is normal to
	 * while the other defines the spin of the camera.
	 * Perhaps another index that defines the cross section we're looking at,
	 * or the "depth".
	 */
	
	
	//TODO:
	private WorldFaces face;
	//private 
	
	
	enum WorldFaces {
		
	}
}
